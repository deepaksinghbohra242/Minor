import Express from 'express';
interface callback {
    (req: Express.Request, res: Express.Response, next: Express.NextFunction): void;
}
export declare const catchAsync: (fn: callback) => (req: Express.Request, res: Express.Response, next: Express.NextFunction) => void;
export {};
