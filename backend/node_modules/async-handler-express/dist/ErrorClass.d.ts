declare class ErrorClass extends Error {
    statusCode: number;
    message: string;
    isOperational: Boolean;
    stack: string;
    constructor(statusCode: number, message: string, isOperational?: Boolean, stack?: string);
}
export default ErrorClass;
